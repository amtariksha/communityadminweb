import { type ReactNode } from 'react';
import VehicleApprovalsContent from './vehicle-approvals-content';

export const metadata = {
  title: 'Vehicle Approvals · Eassy Society',
};

export default function VehicleApprovalsPage(): ReactNode {
  return <VehicleApprovalsContent />;
}
