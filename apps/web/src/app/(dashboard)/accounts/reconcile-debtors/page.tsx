import { type ReactNode } from 'react';
import ReconcileDebtorsContent from './reconcile-content';

export const metadata = {
  title: 'Reconcile Debtors · Eassy Society',
};

export default function ReconcileDebtorsPage(): ReactNode {
  return <ReconcileDebtorsContent />;
}
